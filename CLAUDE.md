# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

**V1 complete** (phases 1–4 + 6 of §88) **plus V1.x modules**: BookmarkModule (§117, trigger `b`), FileModule (§31–33 + §138, trigger `/`, **self-built index, no Everything dependency** — module-owned thread crawls %USERPROFILE% + Desktop/Documents/Downloads known folders + `module.file.index_dirs` (`;`-separated, RestartApplication), then per-root `ReadDirectoryChangesW` watchers keep it fresh (200 ms batching, overflow → full rescan, watcher death → bounded-backoff respawn §146 — 10 s/60 s/300 s then permanent give-up, any successful read cycle resets the consecutive-death counter; death still triggers one full rescan to cover the gap); noise-dir exclusion §120–125 — system/AppData/tool-cache dirs excluded by default, applied twice: crawl-time subtree pruning + query-time file filter; the list lives in the module data file `modules/file/data/excluded-paths.toml` (TOML `excluded` array of literal-string fragments, seeded on first run, re-read on mtime change — no watcher; parse error keeps the previous clause), the settings page has a Path row `module.file.excluded_paths_file` whose Enter opens it in the system default editor, plus Bool master switch `module.file.exclude_noise_paths`; backslash query = verbatim escape hatch (weakened: cannot reach pruned subtrees, §138); query = lowercase substring + `ext:` suffix subset (the rest of Everything syntax retired); ranking (§150) = name-hit tier > score(match-position score per token: exact-stem 26 > prefix 18 > word-start 14 > mid 10, incl. CJK↔ASCII boundaries; + `usage_bonus` §145 — the module now wired in via a two-pass truncate, cheap score top 2×limit re-ranked with usage; − precomputed `name_noise` — Windows dup suffixes `(N)`/`- 副本`/copy markers +6, junk extensions tmp/temp/log/bak/old/part/crdownload/partial/download +8, `~$` Office lock files +8, machine names img_/dsc/mmexport/截图/未命名/新建 +4, version words/v-suffix/date-stamps +3, >3 separator tokens +2/segment, no extension +2, capped 20, directories exempt from suffix items — computed once at index build into `IndexEntry.noise`; garbage suffixes are a blacklist, no common-extension whitelist: usage answers "what this user opens" dynamically) > name_lower lexicographic tiebreak; noise words can never outweigh usage or an extra token hit; first crawl behind a readiness gate; real per-file icons §124 — exe-like keyed per full path, all others per extension, module-owned worker thread + Slot cache, `PresentationInvalidated` repaint on arrival, `present()` stays zero-IO), SystemModule (§126, trigger `>` — fixed-enum system actions: lock/sleep/hibernate/logoff/restart/shutdown/empty-recycle-bin, hand-tabled pinyin/initials/english keys; restart/shutdown get a 30 s abortable native grace period; hibernate hidden when the machine lacks it), WebModule (§143, trigger `g` — fixed search pairs 必应+Edge / Google+Chrome; browser presence gates rows per query like §126 hibernate; usage floats the usual pair to row 0; `g` always searches, even URL-shaped text), and the action menu (§119, `Tab` on a result — §18 secondary actions). The Cargo workspace exists at the repo root with crates under `crates/` (`sakana`, `sakana-core`, `sakana-protocol`, `sakana-ui`, `sakana-windows`, `sakana-util-common`, `sakana-module-app`, `sakana-module-bookmark`, `sakana-module-file`, `sakana-module-system`, `sakana-module-web`). `architecture.md` is the index of the authoritative Product & Architecture Specification (v0.2, written in Chinese) for **sakana**(原名 CUE,§139 起改名), a lightweight Windows launcher — the spec body is split into volumes under `architecture/` (§1–§116, topic files) and `architecture/records/` (§117+, one file per V1.x record, filename = § number); § numbers are globally unique, stable across files, and append-only — it is the source of truth; this file only summarizes its binding decisions (§ references resolve via the file map in `architecture.md`). V1 performance budgets verified per §114 (results recorded at the end of §114).

- **Target platform:** Windows
- **Stack:** Rust + GPUI (Zed's UI framework) + Windows API
- **Product:** `Alt+Space` opens the launcher; plain input searches apps (AppModule — discovery sources: Start Menu + UWP/MSIX + registry App Paths + user-declared portable dirs via `module.app.extra_dirs`, a `;`-separated RestartApplication String setting, §133); `b` + space searches Chromium bookmarks (§117); `/` searches files via the module's own index (§31–33, §138 — coverage = %USERPROFILE% + known folders + `module.file.index_dirs`, kept fresh by per-root watchers); `>` runs fixed system actions (§126 — lock/sleep/hibernate/logoff/restart/shutdown/empty-recycle-bin); `g` + space searches the web (§143 — fixed pairs 必应+Edge / Google+Chrome, up to two rows, arrows pick, usage floats your usual pair to the top); pasting or typing a URL (http/https, bare domain, IP:port — scheme allowlisted) pins an open-link row at the top of the default path — Enter opens it in the default browser (§143); `Tab` on a selected result opens the action menu — App: 打开/以管理员身份运行/打开所在位置 (last two Win32-only), the AppModule open-link row: 打开/用 Edge 打开/用 Chrome 打开 (only when that browser is installed)/复制链接, File: 打开/打开所在文件夹/复制路径, Bookmark: 打开/复制链接, Web: 搜索/复制链接 (§18, §119). **Module triggers are user-customizable** (§128): `module.<id>.trigger` String rows synthesized by Core (routing is Core's, not the module's schema — validation + trim + conflict-check happen in Core's apply flow, never in `module.try_apply_settings`); effective trigger = setting value ?? declared (triggers are required, empty is rejected; a stray empty value in settings.tsv self-heals to the declared default), read fresh per input. The search box no longer forces English (§149 removed §107's layout forcing — the user's IME layout is left untouched on wake/hide), but the launcher window stays **IME-detached** via `ImmAssociateContext(hwnd, NULL)` called once after window creation (v0.6.3 stopgap, §149): GPUI 0.2's Windows backend neither delivers IME composition nor answers its positioning queries, so with a CJK IME active keystrokes went into an unterminated composition and the candidate window popped in the screen corner — detachment makes keys arrive as plain characters, so pinyin/initials and triggers `b` `/` `>` `g` work under any IME state; typed-CJK remains paste-only (§115) until full `WM_IME_COMPOSITION` support is built. sakana is a **single-instance** app (§113): a second process signals the first to show/focus, then exits. **Do-not-disturb mode** (§127): with `core.dnd_mode` on (default), the hotkey's show path is silently suppressed while a fullscreen window is foreground (window rect covers its whole monitor, shell classes excluded); the hide/focus half of the toggle, the tray, and second-instance show are never gated. The tray icon doubles as its state lamp: red rocket = listening, gray = suppressed (host re-evaluates on the foreground-change hook, swaps via NIM_MODIFY only on transitions; Core mirrors the toggle via the `notify_dnd_mode` callback). (Renamed from `core.game_mode` / 游戏模式 2026-08-24; the legacy key is migrated on settings load.) **Icons:** packaged apps get their real logo via `AppDisplayInfo.GetLogo` on the icon worker (AUMID→AppListEntry index handed over before catalog publish, §134); rendered 96px icons are disk-cached per icon key at `modules/app/cache/icons/` and preloaded before catalog publish (§137 — stamp = exe mtime+size / packed package version, RENDER_EPOCH for render-pipeline changes, GC deletes stamp-mismatched files of catalog keys only, never unknown keys); fallback icons everywhere (system actions, app/folder/file/bookmark placeholders) are module-rendered Segoe Fluent/MDL2 glyph bitmaps (`sakana-util-win::glyph`, §135) — `SystemIconId` / `ResultIcon::SystemIcon` are retired, the protocol carries Raster only, never emoji. **Lock-keys service: REMOVED entirely (§148)** — the WH_KEYBOARD_LL hook, press-edge Ledger, OSD card window (the former second GPUI window), `core.lockkeys.*` settings rows, and the `SessionReset` host message are all gone; `WM_WTSSESSION` lock still reports `FocusLost` (§115), stale `core.lockkeys.*` lines in settings.tsv are ignored at load and self-heal on the next whole-file rewrite.

## Commands

Cargo workspace at repo root, crates under `crates/`, all named `sakana-*`:

- Build: `cargo build` (release: `cargo build --release`)
- Run the launcher: `cargo run -p sakana`
- Test everything: `cargo test`
- Test one crate: `cargo test -p sakana-module-app`
- Run a single test: `cargo test -p sakana-module-app <test_name>` (add `-- --nocapture` for output)
- Lint / format: `cargo clippy --all-targets` / `cargo fmt`
- Note: a running `sakana.exe` locks the binary — `Stop-Process -Name sakana` before rebuilding.
- Package for distribution: `scripts/package.ps1` (release build → `dist/sakana-setup-<ver>.exe` via Inno Setup 6 + `scripts/setup.iss` — the wizard setup.exe is the ONLY supported install form, no zip/portable path; `-Sign` signs both `sakana.exe` and the setup via `scripts/sign.ps1` — self-signed dev cert only proves integrity, public distribution needs an OV cert). Installer behavior: per-user (免管理员), zh-CN (vendored `scripts/lang/ChineseSimplified.isl`, from the matching Inno tag) + en via `ShowLanguageDialog=auto`, detects a running sakana via the §113 mutex (`AppMutex=Local\sakana.SingleInstance`), HKCU uninstall entry, optional 开机自启 task writes the same HKCU Run key as the in-app toggle. `core.start_on_boot` (§36) is the in-app autostart toggle — host callback writes the HKCU Run key, same §53/§112 sync-exception pattern as `apply_hotkey`.
- Brand icon: `assets/sakana.svg` → `assets/sakana.ico` via `scripts/icon.ps1` (Edge headless rasterization, no third-party tools; re-run when the SVG changes). Embedded into sakana.exe by `crates/sakana/build.rs` (embed-resource, resource id 1); tray + launcher window load it through `sakana-windows::icon`, installer shortcut inherits the exe icon.

## Architecture

The one rule that governs every placement decision (§3):

> **Core owns how features run; Modules own how features work.**

- **Core** is a thin host runtime: session lifecycle, input routing (it parses *only* the module trigger prefix — `ext:pdf`-style query syntax belongs to modules), module registry, query lifecycle, result state/selection, settings host, usage store. Core must not know what a `.lnk`, pinyin, Everything, a fuzzy score, or app dedup is.
- **Modules** are trusted, built-in, statically linked Rust crates (no plugin system) implementing the `Module` / `LauncherModule` traits. They own all business semantics: search, ranking, icons, launch/open.
- **UI (GPUI)** and **Windows host** (RegisterHotKey, shell execute, monitor placement, single-instance mutex, tray icon §116) are separate crates; modules never touch them.
- **The `sakana` binary crate is the orchestrator** (§112): host/UI events → Core state transitions → platform-neutral `CoreEffect { ShowLauncher, HideLauncher, FocusInput }` → executed by `sakana-ui` / `sakana-windows`. Sole synchronous exception: injected host callbacks (`apply_hotkey` / `apply_start_on_boot` for settings try-apply, `open_path` for Path-row activation, `fullscreen_probe` for the §127 game-mode gate, §42, §53, §122, §127) — functions, not a HostPlatform trait.

Binding contracts (§86 is the canonical interface):

- Modules hand Core **owned opaque** `ModuleItem` handles: `id: ItemId` + `payload: Arc<dyn Any + Send + Sync>` (§11). Core never calls `downcast_ref` — item lifetime is expressed by `Arc` ownership, so there is no `HashMap<ItemId, Entry>` lookup table, no eviction timing, no UI-thread lock. A Core result struct with `exe` / `file_path` / `url` fields is explicitly forbidden (§12).
- Modules never call back into Core (`close_launcher()` etc.). They return `ModuleOutcome { status, session: Close | KeepOpen, usage }` and Core acts on it (§21–23).
- Exactly one default module (App, no prefix); triggers must not conflict; no-prefix input always means App (§82). **AppModule is a required module in V1 — it cannot be disabled** (§65).
- Modal isolation: each module returns only its own result type. No universal mixed result list, no cross-module ranking (§83).
- Composition root: only the top-level `sakana` crate names concrete modules. `sakana-core` must never `use sakana_module_app::...` (§70–71). Dependency direction: `sakana → {sakana-core, sakana-ui, sakana-windows, sakana-module-*}`, `sakana-module-* → sakana-protocol` (+ `sakana-util-common`/`sakana-util-win` as needed — system currently needs neither), `sakana-util-common → sakana-protocol`, `sakana-util-win → sakana-protocol`, `sakana-core → sakana-protocol`, `sakana-ui → {sakana-core, sakana-protocol}`. The registry stores `Box<dyn LauncherModule>` directly (no trait-upcasting problem, §5.3).
- Async model (§91–106): Core is a single-threaded state machine on the UI thread. North star (§91): **Core never cancels async work; it judges result validity via `QueryTicket { session_id, module_id, module_epoch, generation }`** — modules bound their own resource use (Everything: dedicated IPC thread + latest-wins slot, §99). `generation` is Core bookkeeping and is *not* echoed by modules: `QueryResponse` carries only `items`; the ticket is captured by Core's spawn wrapper and completions re-enter through one event queue (§96). Input change clears results + selection immediately — stale results must never be activatable (§102). Activation outcomes: usage is always recorded, but session disposition applies only if the originating session is still current (§103). Futures are `Send + 'static`, created without blocking/IO, polled by an injected `TaskSpawner` (GPUI in production, manual pump in tests). No debounce, no loading state, **no `catch_unwind` panic boundary** (§104 — §63 discipline instead).
- Presentation (§13–17, §108–109): protocol types use `Arc<str>`, never GPUI's `SharedString` (§71). `ResultIcon` is a protocol-owned `Raster` bitmap: RGBA8, row-major, sRGB, straight alpha, `len == w*h*4`, single 96px size (UI downscales and converts at texture upload). UI caches GPU textures by `Arc` pointer — a module must reuse the same `Arc<IconImage>` per cached icon. Result Row is one fixed grid with optional slots (icon gutter always reserved) — no second layout. Modules push `ModuleEvent::PresentationInvalidated { items }` via `ModuleContext.events` (sink bound to `ModuleEpoch` at load; stale-epoch events dropped) when async resources (icons) arrive; Core re-runs `present()` on the visible rows.

Workspace layout (§68): `crates/{sakana, sakana-core, sakana-protocol, sakana-ui, sakana-windows, sakana-util-common, sakana-util-win, sakana-module-app, sakana-module-bookmark, sakana-module-file, sakana-module-system, sakana-module-web}`. `sakana-util-common` is the module-shared platform-neutral helper crate (Rule of Three sink-down, §72–73, §145: currently just the `usage_bonus` ranking formula shared by app/bookmark/web — system deliberately stays out, §126 cap design). `sakana-util-win` is the module-shared Win32 helper crate (Rule of Three sink-down, §72–73: ComGuard, system-icon extraction, shell_execute + §119 runas/reveal/clipboard, the §143 `browser` family — Browser enum/exe-path discovery/open_url/browser icons, sunk from bookmark's private `chromium::Browser` on its third+ use) — shared code sinks down there, never up into Core. The `sakana-` prefix avoids collisions with Rust's `core` and the official `windows` crate.

Settings and storage:

- Settings namespaces: `core.*` and `module.<module-id>.*`. Modules declare a schema (each `SettingSpec` carries an `apply_policy`, §38); Core renders all settings UI (§41 — entry: tray menu "设置"; Core emits the row model, sakana-ui only renders; edit UI covers Bool + Hotkey kinds; String and Integer rows get inline text editing (Enter enters edit mode with the buffer prefilled, Enter commits the transaction, Esc cancels — a view-local mode like hotkey capture, §128; Integer rows carry a spec-owned range and report parse/range errors in the row, §141); Enum rows carry spec-owned stable values + Chinese labels and cycle with Enter/←→ (§141), and a Path row's Enter = open the path in the system default app via the `open_path` host callback — not a value change, no transaction, §122). Settings live only in the Settings Host, never in module storage (§48 — but a list meant to be hand-edited, like the file-search exclusion list, is module *data* in `modules/<id>/data/`, not a setting value, §122; **hand-editable config files are always TOML**, §123). Persisted as `<storage_root>/settings.tsv` (header-versioned, whole-file rewrite, tmp+rename; corrupt lines skipped).
- **Settings changes are transactional** (§42): validate → try-apply (`Module::try_apply_settings`, or the `apply_hotkey` host callback for `core.hotkey`) → commit in-memory → persist. Failure: no commit, UI restores the old value. `RestartApplication` class: validate → commit → persist → mark `restart_required`.
- Usage store is **aggregated** (§50): `UsageStat { count, last_used }` keyed by `(ModuleId, ItemKey, ActionId)` — no unbounded event log. `item_key` is a stable launch identity (§51): AUMID for packaged apps, canonical exe + raw args + working dir for Win32 (§141 — args keep their case/quoting/whitespace, only the path part is case-normalized). Persisted as `<storage_root>/usage.tsv` (header-versioned, whole-file rewrite by a dedicated writer thread behind a capacity-1 coalescing queue — in-memory commit is immediate, disk write is async, explicit flush on quit, §141); corrupt lines are skipped, never panic.
- Diagnostics log (§132): `%LOCALAPPDATA%\sakana\sakana.log` — global sink in `sakana-protocol::log` (std-only, platform-neutral) with a `logln!` macro replacing `eprintln!` everywhere except test helpers. Bounded: rotates to `sakana.log.old` at 1 MB, one generation. A dedicated `log-writer` thread carries all file IO — callers only `format!` + channel send; **no fsync** (page cache survives process crash; power-loss durability is not the goal). Open failure falls back to stderr-only and never panics. The settings page has a read-only Path row `core.log_file` whose Enter opens the log in the default app (§122 mechanism).
- Module storage root: `%LOCALAPPDATA%\sakana\` with `modules/<id>/{data,state,cache}` — data (durable user data), state (recoverable), cache (freely deletable) (§43–47).

## Hard "do not build" list (§26, §69, §76)

Do not create, even as scaffolding: plugin-sdk / plugin-runtime / dynamic loading / DLL ABI / WASM / module sandbox; universal-search abstractions (`SearchDocument`, `UniversalRanker`, `CandidateProvider`, `UniversalIndex`, search/ranking strategy frameworks); an admin-level full-disk NTFS index (USN journal / raw MFT read — runtime elevation, rejected per §138's cost analysis; the user-mode scoped index in FileModule *is* the sanctioned form, and it is not a general-purpose indexer); JSON-RPC; clipboard manager (history/listening — the §18 single-shot Copy path/link actions in §119 are not this); calculator; shell runner; network search (narrowed by §143: *fetching* web results into the launcher is still banned — opening URLs and delegating searches to the system browser via WebModule is the sanctioned form, sakana itself does zero network IO); AI features. Also absent by design in V1: query cancellation tokens, a panic boundary, loading indicators, app-catalog file watchers.

The internal Rust traits are an internal architecture boundary, **not** a third-party plugin ABI (§67).

## Working rules

- **Anti-over-abstraction is a product requirement.** Rule of Three: write it inline the first time, allow duplication the second, extract a shared util only the third — and shared code sinks *downward* into a util crate, never up into Core (§72–73).
- Unsure where logic belongs? Apply §87: feature semantics (pinyin, `.lnk`, file index, AUMID, file paths) → module; needed by several modules → duplicate first, then shared util; controls launcher lifecycle (window, session, routing, settings, usage, query) → Core.
- Modules share Core's process: never `unwrap()` external data; IO / Windows API failures must return `ModuleError`, not panic (§63).
- App discovery in V1 = User/Common Start Menu + UWP/MSIX only; do not scan the disk for `.exe` (§29). Packaged apps via `PackageManager` → `GetAppListEntriesAsync()` → `AppListEntry` (Package ≠ App), launched via `IApplicationActivationManager::ActivateApplication(AUMID)` — never AppsFolder enumeration (§29). Dedup by launch semantics (AUMID; canonical exe + raw args + working dir, §141) — prefer duplicates over aggressive dedup (§30). App catalog refreshes at process start only — no watchers; newly installed apps appear after a sakana restart (§56). Per the §56 spike (cold discovery ≈ 6.7 s), catalog build runs on the module's own thread — `load()` stays cheap, query futures wait on a one-shot readiness gate, no disk cache.
- Performance budgets (§55, §77–79, measured per the contract in §114): hotkey → usable < 100 ms (ideal < 50 ms, WM_HOTKEY → first keystroke accepted); cold start < 500 ms (process entry → hotkey registered + ready); app search P50 < 5 ms / P95 < 15 ms (InputChanged → ResultState committed, excluding GPU present); resident memory < 100 MB (Private Working Set, idle 60 s). Never do app discovery, registry queries, icon extraction, or index crawling on the wake hot path (§55).
- No platform-specific code in `sakana-core` / `sakana-protocol` without demonstrated benefit (§110–111): no `std::os::windows`, `windows`-crate types, or Win32 calls — enforced by `scripts/check-arch.ps1` (dependency direction + platform purity + composition root; runs in CI §147 alongside fmt / clippy `-D warnings` / `cargo test`); burden of proof is on whoever introduces it. This also covers **`core.*` setting values**: they must be OS-neutral data (e.g., `Hotkey { modifiers, key }`, never Win32 `MOD_*` / `VK_*` constants, §53); platform translation lives in the host. Platform code is quarantined in `sakana-windows/` and module internals. Cross-platform (macOS) is architecturally unblocked but deliberately un-abstracted until a real second-platform effort starts.
- UX invariants (§115): empty query shows usage Top Apps; new non-empty results select row 0; input change clears results/selection in Core (stale results never activatable) **but the view keeps painting the previous rows while the next query commit is in flight (`results_pending`) — "No results" only from a committed-empty result set, never flashed between keystrokes** (§115 amendment 2026-08-25, bug 3: the clear-then-repopulate strobe read as "divider jitter"); failed activation keeps the session open with an error; Unicode paste works (IME composition delivery via GPUI is untested since §149 removed the forcing); `core.hide_on_focus_loss` governs focus-loss hiding — and workstation lock counts as focus loss (the host re-posts `FocusLost` on `WM_WTSSESSION` lock/console-disconnect, the documented channel; whether the WinEvent foreground hook fires on lock is undocumented and build-dependent; unlock must never auto-show). **Only Core's show sequence may make the window visible** — GPUI 0.2.2 unconditionally `ShowWindow`s on `WM_DISPLAYCHANGE` when the recorded monitor disconnects (unplug → accidental wake with a session-less snapshot); the host subclasses the launcher window and swallows `WM_DISPLAYCHANGE` while hidden (§115 amendment 2026-08-25) — and swallows `WM_CLOSE` unconditionally: the launcher has no close semantics, so Alt+F4 / taskbar close would drop the root view (and the `Core` it owns), leaving a zombie process with no UI, dead hotkey/tray/mutex, and no way to quit (§142). **Window sizing is by client area, not window rect** — `SetWindowPos` sizes include the DWM invisible frame (22×13 px at 150%); feeding the 640×450 design size shrunk the client to 441.33 logical so a full 5-row list overflowed by 2.5px and flex-shrink compressed the input row and divider ("divider thickens/shifts under 5 results" was really "compressed at exactly 5"); `place_on_active_monitor` measures `GetWindowRect`−`GetClientRect` after the DPI-syncing first step and inflates the second so the client is exactly the design size (§115 amendment 2026-08-25, bug 3b). The tray icon (§116) is the only persistent presence signal and V1's only quit path (left-click = show, right-click menu = Show/Settings/Quit).
- UI stays on **crates.io `gpui` 0.2.x with hand-rolled styled-div views** — gpui-component/gpui-kit was evaluated and rejected (2026-09): it pins its own `gpui-pre` snapshot (apps must depend on gpui-kit, never gpui itself), so adopting it swaps the whole UI stack and forces re-validation of every hard-won §115/§130 window/DPI/IME behavior; a launcher's UI (input + list + settings page) uses ~1% of the component library. Revisit only if UI scope genuinely grows.

## Development order (§88)

Follow the phases strictly — later phases assume earlier ones exist:

1. **Shell** — Alt+Space, GPUI window, input, Esc/Enter/↑↓, **single instance**, driven by a fake module. Includes two spikes: IME forcing approach (§107 — the Win32 calls are candidates to validate, GPUI maintains its own IME state) and `AppModule::load` cold-start timing (§56).
2. **Module Protocol** — `Module` / `LauncherModule` traits, registry, routing, presentation, action, outcome; validate with a DemoModule that proves Core has no business coupling.
3. **AppModule** — Start Menu + packaged app discovery (AppListEntry path — §29), launch, icons, fuzzy matching, pinyin (full + initials), ranking.
4. **Usage** — frequency/recency recording feeding ranking.
5. **FileModule** — `/` trigger, open file/folder. ~~Deferred out of V1~~ **Landed in V1.x**; §138 (2026-09) retired the Everything dependency in favor of the module's own scoped index + watchers — §118 is the superseded Everything IPC record, kept for history.
6. **Settings** — schema-driven settings UI with transactional apply (§42), only after real module settings are known. Do not build a settings framework in Phase 1.
